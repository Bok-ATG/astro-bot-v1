{
  "name": "slack-openai-assistant-bot",
  "version": "1.0.0",
  "description": "A Slackbot that connects to OpenAI Assistants API and responds to channel posts.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "debug": "node --inspect index.js",
    "docker:dev": "docker compose up --build",
    "docker:build": "docker build -t astrobot ."
  },
  "dependencies": {
    "@slack/bolt": "^3.19.0",
    "axios": "^1.10.0",
    "chalk": "^4.1.2",
    "dotenv": "^16.4.5",
    "node-cron": "^3.0.3",
    "openai": "^4.21.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
