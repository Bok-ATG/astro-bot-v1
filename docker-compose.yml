services:

  bot:
    container_name: astro-qa-bot
    restart: always
    user: "1000:1000"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    image: local-astro-qa-bot
    build:
      context: .
      dockerfile: ./Dockerfile
    env_file:
      - .env
    volumes:
      - type: bind
        source: ./index.js
        target: /app/index.js
      - ./src:/app/src